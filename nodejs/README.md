nodejs
======

CentOS 6.5 with NodeJS and the `n` node version manager

Prerequisites
-------------

- VirtualBox
- Vagrant
- Ruby
- Bundler

  ```
  gem install bundler
  ```

Usage
-----

```
bundle install
bundle exec berks install -p cookbooks
vagrant up
```
